<%= pkg.name %>
========================

[<%= pkg.name %>] is the Node.js based Simple Live/Auto Reloading Web Server.
  - Reload statically on update the html files
  - Refresh dynamically on update the files like css, js, png, and etc.
  - No browser extensions but the WebSocket needed.

Command Line Usage
----
```sh
autoreload [root-dir [port]]
```
#### Example

```sh
autoreload ./site-files 8008
```

Module Usage (Example)
----
```
var launcher = require('<%= pkg.name %>');

var server = launcher({
  port: 8008,
  root: './',
  listDirectory: true,
  watch: /.(png|js|html)$/i
  forceReload: [/.json$/i, "static.swf"]
});
```

#### Option

See 'src/lib/default-options.ls' for the details of options.

Version
----
v<%= pkg.version %>

Installation
--------------
You can install this package via 'npm'.

License
----
<%
  pkg.licenses.forEach(function(l){
    %>[<%= l.type %>]
<%
  });
%>

[<%= pkg.name %>]:<%= pkg.homepage %>
<%
  pkg.licenses.forEach(function(l){
    %>[<%= l.type %>]:<%= l.url %>
<%
  });
%>



